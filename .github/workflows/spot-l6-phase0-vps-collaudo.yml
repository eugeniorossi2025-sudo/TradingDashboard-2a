name: DASH2A Spot L6 Phase0 VPS Collaudo

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Type RUN_SPOT_L6_PHASE0 to execute spot-l6-phase0-vps-collaudo.ps1"
        required: true
        type: string

permissions:
  contents: read

jobs:
  collaudo:
    name: Spot L6 Phase 0 on Decisore VPS
    if: ${{ github.event.inputs.confirm == 'RUN_SPOT_L6_PHASE0' }}
    runs-on:
      - self-hosted
      - Windows
      - DASH2A
      - DASH2A-DECISORE
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Phase 0 VPS collaudo
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $env:DECISORE_URL = 'http://127.0.0.1'
          & "$env:GITHUB_WORKSPACE\tools\spot-l6-phase0-vps-collaudo.ps1"
          if ($LASTEXITCODE -ne 0) {
            throw "Spot L6 Phase 0 VPS collaudo FAILED"
          }
          Write-Host "SPOT_L6_PHASE0_VPS_COLLAUDO_PASS"
