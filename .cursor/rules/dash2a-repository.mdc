---
description: Map "Dash due" requests to the correct Dashboard 2 repository
alwaysApply: true
---

# Dashboard 2 Repository Memory

When the user says "Dash due", "Dashboard 2", "Dash2", "DASH2", or refers to the Dashboard 2 frontend, use only this local repository:

```text
C:\Users\eugen\Desktop\NuovaDashboard-MarcoTurri
```

Before modifying anything, verify:

```bash
git remote -v
git branch --show-current
git status --short
```

The expected remote is:

```text
origin: https://github.com/eugeniorossi2025-sudo/TradingDashboard-2a.git
```

Frontend work belongs under:

```text
frontend/
```

Do not use `TradingDashboard-iis`, `TradingDashboard`, Dashboard 1 frontend, Dashboard 1 Firebase, or Dashboard 1 SQL as source of truth for DASH2A work.

## Dashboard 2 Operational Map

Use this map unless the user explicitly updates it:

```text
Frontend pubblico DASH2A: https://eugenio-dashboard-2a.web.app/auth/login?redirect=/pages/user
Firebase project: eugenio-dashboard-2
Local frontend: http://localhost:5001
Local WebApi: http://localhost:5299
Backend/API operativo: http://51.83.159.175
Operative Dashboard.Url: http://51.178.16.37
Operative Dashboard.UrlDev: http://192.168.1.41:5286
Operative Dashboard.Username: eugenio
WebApi SQL Server: 51.83.159.175:1433
Decisore SQL Server: 51.210.181.37:1433
```

The operative Gamebot Dashboard 2 config in this repo is the source of truth for bot-to-dashboard routing:

```text
tools/eugenio-gamebot/source/Gamebot/app.config
tools/eugenio-gamebot/release-reference/Gamebot.exe.config
tools/eugenio-bot/Gamebot/app.config
```

Current credential finding:

```text
WebApi SQL login sa2 can connect to master on 51.83.159.175, but does not have access to Eugenio-Demo10.
HAS_DBACCESS('Eugenio-Demo10') = 0 for sa2.
Do not assume local WebApi can read real SQL data until this mapping is fixed or another valid connection string is provided.
```

Keep Dashboard 2 credentials and references separate from Dashboard 1:

```text
Dashboard 2 frontend identity: eugenio-dashboard-2a
Dashboard 2 app login: admin
Dashboard 2 WebApi SQL login reference: sa2 on 51.83.159.175
Dashboard 2 Decisore SQL login reference: sa on 51.210.181.37
```

Do not use Dashboard 1 frontend, Dashboard 1 Firebase, Dashboard 1 SQL login, removed backend domains, old endpoints, or dirty fallback URLs as the source of truth for DASH2A.

Do not deploy unless the user explicitly asks for deploy.
